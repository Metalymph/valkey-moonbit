// test "hello" {
//   if @lib.hello() != "Hello, world!" {
//     fail("@lib.hello() != \"Hello, world!\"")
//   }
// }

///|
test "test_valkeyclient_new" {
  // let client = @lib.ValkeyClient::new()
  // assert_eq(client.host == "localhost")
  // inspect(client.host, content="localhost")
}
